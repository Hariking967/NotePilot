// Supabase client was added earlier but reverted per user request.
// If you need to re-enable Supabase, recreate this file with a valid
// NEXT_PUBLIC_SUPABASE_KEY and the createClient call.

export const supabase = undefined as unknown as any;
