// uploadPpt was added earlier. It's been neutralized per user request.
export async function uploadPpt(_file: File) {
  throw new Error(
    "uploadPpt has been reverted — this helper is no longer available"
  );
}
